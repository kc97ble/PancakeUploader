html
  head
    style(type="text/css").
      html {
        background-color: #AA6C39;
      }
      body {
        background-color: #FFEFE1;
        padding: 1em 1em;
        border-radius: 1em;
        margin: 2em auto;
      }
      @media screen and (min-width: 480px) {
        body {
          max-width: 50%;
        }
      }
  body
    h1(style="text-align: center") Pancake Uploader
    form(action="upload", method="post", enctype="multipart/form-data")
      div
        input(type="file", style="width: 100%", name="file")
      div(style="text-align: center; padding: 2em 2em;")
          button(type="submit", style="width: 50%; padding: 1em 1em") Submit
